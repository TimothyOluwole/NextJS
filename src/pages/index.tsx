import {Layout} from '@/features/layout/Layout'

const HomePage = (): React.ReactElement => {
  return (
    <Layout>
      <h1 className="mb-8 text-2xl font-bold">Home</h1>

      <p>Welcome to Vialto</p>
    </Layout>
  )
}

export default HomePage
